[project]
name = "shared"
version = "0.1.0"
description = "Shared utilities, DTOs, and models for the Python monorepo"
readme = "README.md"
authors = [
    { name = "Avinash Nishad", email = "js.avinash.nishad@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.6.0",
]